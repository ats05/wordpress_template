# WordPress環境構築用docker-compose

- `$ git clone (このリポジトリ)`
- `$ cp docker/.env.sample docker/.env`
- 必要に応じ`docker/.env`をイイカンジに編集
- `$ cd docker`
- `$ docker-compose build`
- `$ docker-compose up -d`
- `localhost:8080`にアクセス（`docker/.env`で変更してなければ）

↓
```
＿人人人人人人＿
＞　おいしい　＜
 ￣Y^Y^Y^Y^Y￣
 ```